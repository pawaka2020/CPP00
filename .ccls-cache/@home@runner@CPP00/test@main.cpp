#include "Person.hpp"

int main() {
    Person person("John", 30); // Create a Person object with name "John" and age 30
    person.doSomething(); // Call the doSomething() function on the Person object
    return 0;
}